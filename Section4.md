# Next.js 라우팅 기본

## Next.js 라우팅 방식
- Next의 라우팅 방식에는 크게 2가지가 있다.
  1. App Router
  2. Page Router

### 1. App Router
- app 폴더를 기반으로 라우팅 방식을 제공하기 때문에 App Router라고 불립니다.
- React 서버 컴포넌트 기반으로 구성된 라우터이며 공용 레이아웃, 중첩 라우팅, 로딩 상태 제어, 에러 처리 등의 편의 기능을 제공합니다.

### 2. Page Router
- 프로젝트 루트 레벨에 pages 폴더를 생성하고 React 컴포넌트 파일을 추가하면 자동으로 파일 기반의 라우팅이 구성됩니다.

### App Router와 Page Router 차이점
- 앱 라우터는 같은 관심사를 최대한 가까이 배치할 수 있음
- 페이지 라우터는 pages, layouts, components, libs 등의 폴더가 항상 별도로 구성되어 있어 파일 간에 참조된 코드를 보느라 여기저기 뒤져야 하는 비용이 크다.
- 반면, 앱 라우터는 app 이라는 폴더 안에 특정 라우팅과 관계된 페이지 컴포넌트(page.js) 바로 옆에 레이아웃, 컴포넌트, 라이브러리 파일들을 배치할 수 있게 되어 더 파일 배치가 쉬워졌다.

---

## 페이지 라우팅 기본 및 리액트 컴포넌트와 JSX 기초 설명

### JSX (JavaScript Extension)
- 자바스크립트 확장 문법
- 자바스크립트 안에 HTML, CSS까지 다 표현할 수 있는 방법

### pages
- `/pages` 하위에 있는 jsx파일은 모두 **소문자**로 작성해야한다.

---

## 페이지 라우팅 구성 후 컴포넌트 구성

### Page Routing
- 파일을 기반으로 자동으로 라우팅이 생성되는 것

---

## Link 컴포넌트 설명

### Link 컴포넌트
- Next는 <Link> 라는 컴포넌트를 제공
- 이 컴포넌트는 `<a>`와 같이 페이지를 이동할 때 사용

```jsx
import Link from 'next/link'

function Home() {
    return (
        <div>
          <Link href={"/"}>Main</Link>
          <Link href={"/login"}>Login</Link>
        </div>
    )
}
```

위 코드가 브라우저에서 실행되면 최종적으로 다음과 같은 태그 구조 생성

```html
<div>
  <a href="/">Main</a>
  <a href="/login">Login</a>
</div>
```

### Link 컴포넌트 장점
- `<Link>` 컴포넌트로 페이지를 이동하면 마치 싱글 페이지 애플리케이션(Single Page Application)의 라우터처럼 이동하게 된다.
- 보통 페이지를 이동할 때 해당 페이지 정보(HTML)를 서버에 요청하고 받아와 그려야 하는데, 싱글 페이지 애플리케이션 방식을 이용하게 되면 클라이언트 레벨에서 페이지를 이동할 수 있게 되기 때문에 좀 더 페이지간 전환이 빠르고 매끄럽다.


- `<Link>` 컴포넌트가 위치한 UI영역이 뷰포트(viewport)에 잡히면 넥스트 내부적으로 해당 페이지 데이터를 미리 요청해둔다.
- 이걸 prefetching 이라고 하는데, 미리 다음 페이지 데이터가 요청되어 있다면 클릭했을 때 거의 즉시 페이지 로딩이 일어날 수 있다.


- 이러한 장점들 때문에 `<a>`보다는 `<Link>`컴포넌트를 사용하는 걸 권장한다.