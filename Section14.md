# 실전 프로젝트 그 이후

## Hydration 소개
- 하이드레이션이란 서버에서 그려준 HTML 파일에 리액트가 해당 HTML 코드의 DOM을 제어할 수 있도록 붙여주는 작업

## 앱 라우터와 페이지 라우터의 차이점
- 앱 라우터는 같은 관심사를 최대한 가까이 배치
- 페이지 라우터는 `pages` `layout` `components` `libs` 등의 폴더가 항상 별도로 구성되어 있어 파일 간에 참조된 코드를 보느라 여기저기 확인해야 하는 비용이 발생
- 반면, 앱 라우터는 `app` 이라는 폴더 안에 특정 라우팅과 관계된 페이지 컴포넌트(page.js) 바로 옆에 필요한 레이아웃, 컴포넌트, 라이브러리 파일들을 배치할 수 있게 되어 더 파일 배치가 쉬워짐